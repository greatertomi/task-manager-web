import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#705CFF',
    },
    text: {
      secondary: 'white',
    },
  },
  typography: {
    fontFamily: "'Ubuntu', sans-serif",
  },
});
